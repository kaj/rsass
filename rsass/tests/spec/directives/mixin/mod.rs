//! Tests auto-converted from "sass-spec/spec/directives/mixin"

fn runner() -> crate::TestRunner {
    super::runner().with_cwd("mixin")
}

mod comment;

mod custom_ident_include;

mod double_underscore_name;

mod whitespace;
