//! Tests auto-converted from "sass-spec/spec/core_functions/selector/is_superselector/simple/pseudo/selector_arg"

#[allow(unused)]
fn runner() -> crate::TestRunner {
    super::runner().with_cwd("selector_arg")
}

mod any;

mod current;

mod has;

mod host;

mod host_context;

mod is;

mod matches;

mod not;

mod nth_child;

mod nth_last_child;

mod slotted;

mod test_where;
