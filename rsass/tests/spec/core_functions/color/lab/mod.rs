//! Tests auto-converted from "sass-spec/spec/core_functions/color/lab"

#[allow(unused)]
fn runner() -> crate::TestRunner {
    super::runner().with_cwd("lab")
}

mod alpha;

mod error;

mod no_alpha;

mod relative_color;

mod special_functions;
