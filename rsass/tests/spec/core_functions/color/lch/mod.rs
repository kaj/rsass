//! Tests auto-converted from "sass-spec/spec/core_functions/color/lch"

#[allow(unused)]
fn runner() -> crate::TestRunner {
    super::runner().with_cwd("lch")
}

mod alpha;

mod error;

mod no_alpha;

mod special_functions;
