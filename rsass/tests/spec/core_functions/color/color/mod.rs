//! Tests auto-converted from "sass-spec/spec/core_functions/color/color"

#[allow(unused)]
fn runner() -> crate::TestRunner {
    super::runner().with_cwd("color")
}

mod alpha;

mod degenerate;

mod error;

mod no_alpha;

mod relative_color;

mod spaces;

mod special_functions;
