//! Tests auto-converted from "sass-spec/spec/values/calculation/calc"

#[allow(unused)]
fn runner() -> crate::TestRunner {
    super::runner().with_cwd("calc")
}

mod error;

mod no_operator;

mod operator;

mod simplify;
