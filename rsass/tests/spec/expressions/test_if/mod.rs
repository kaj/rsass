//! Tests auto-converted from "sass-spec/spec/expressions/if"

fn runner() -> crate::TestRunner {
    super::runner().with_cwd("if")
}

mod css;

mod test_else;

mod error;

mod raw;

mod sass;

mod short_circuit;

mod syntax;
