//! Tests auto-converted from "sass-spec/spec/variables"

fn runner() -> crate::TestRunner {
    super::runner().with_cwd("variables")
}

mod comments;

mod double_flag;

mod semi_global;

mod whitespace;
