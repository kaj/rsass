//! Tests auto-converted from "sass-spec/spec/non_conformant/errors/import/miss/mixin/control-else"

fn runner() -> crate::TestRunner {
    super::runner().with_cwd("control-else")
}

mod inside;

mod outside;
