//! Tests auto-converted from "sass-spec/spec/non_conformant/errors/import/file/mixin/control-if"

fn runner() -> crate::TestRunner {
    super::runner().with_cwd("control-if")
}

mod inside;

mod outside;
