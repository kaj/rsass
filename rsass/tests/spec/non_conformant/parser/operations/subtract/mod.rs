//! Tests auto-converted from "sass-spec/spec/non_conformant/parser/operations/subtract"

fn runner() -> crate::TestRunner {
    super::runner().with_cwd("subtract")
}

mod dimensions;

mod numbers;

mod strings;
