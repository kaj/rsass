//! Tests auto-converted from "sass-spec/spec/arguments"
#[allow(unused)]
use super::rsass;

// From "sass-spec/spec/arguments/inovcation.hrx"
mod inovcation {
    #[allow(unused)]
    use super::rsass;
    mod function {
        #[allow(unused)]
        use super::rsass;
        mod error {
            #[allow(unused)]
            use super::rsass;

            // Ignoring "positional_after_named", error tests are not supported yet.
        }
    }
    mod mixin {
        #[allow(unused)]
        use super::rsass;
        mod error {
            #[allow(unused)]
            use super::rsass;

            // Ignoring "positional_after_named", error tests are not supported yet.
        }
    }
}
